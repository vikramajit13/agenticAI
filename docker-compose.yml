version: "3.9"

services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: docker
      LOG_LEVEL: INFO
      LLM_MODEL: llama3.1:8b
      OLLAMA_BASE_URL: http://host.docker.internal:11434
